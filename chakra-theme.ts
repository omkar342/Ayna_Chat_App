import { extendTheme } from "@chakra-ui/react";

const colors = {
  brand: {
    primary: "#FFB200",
    secondary: "#da9b66",
    tertiary: "#B83280",
    quaternary: "#F9F9F9",
  },
};

export const theme = extendTheme({ colors });
